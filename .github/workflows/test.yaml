name: Test
on:
  - push
  - pull_request

jobs:
  commit-check:
    name: Commit Check
    runs-on: ubuntu-latest
    steps:
      - name: Commit Check
        uses: IceflowRE/gitcc@751bf777693ddbd699daf29f0cf7bc3fd9dfaa28
        with:
          validator: "simpletag"
